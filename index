<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تواصل مع المطور: سعد أبو شندي</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        :root {
            /* الألوان مستوحاة من ثيم مشروعك Material-UI */
            --primary-color: #024684; /* أزرق داكن */
            --secondary-color: #a7a9ad; /* رمادي */
            --success-color: #3EB75E; /* أخضر */
            --text-primary: #192335; /* أسود مزرق */
            --text-secondary: #6b7385; /* رمادي أغمق */
            --background-default: #f9f9f9;
            --background-paper: #ffffff;
            --whatsapp-green: #25D366;
            --email-red: #DB4437;
        }

        body {
            font-family: 'Cairo', sans-serif;
            margin: 0;
            padding: 40px 20px;
            background-color: var(--background-default);
            color: var(--text-primary);
            line-height: 1.6;
            direction: rtl;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .contact-card {
            background-color: var(--background-paper);
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0px 8px 25px rgba(37, 47, 63, 0.15); /* ظل احترافي وبارز */
            text-align: center;
            max-width: 500px;
            width: 100%;
            transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
        }

        .contact-card:hover {
            transform: translateY(-8px);
            box-shadow: 0px 12px 35px rgba(37, 47, 63, 0.25); /* ظل أكبر عند التحويم */
        }

        .developer-name {
            font-size: 2.2em;
            font-weight: 700;
            color: var(--primary-color);
            margin-bottom: 20px;
        }

        .contact-info {
            margin-top: 30px;
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .contact-item {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 15px;
            font-size: 1.1em;
            font-weight: 600;
            color: var(--text-primary);
            text-decoration: none;
            padding: 15px 25px;
            border-radius: 8px;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        .contact-item:hover {
            transform: translateY(-3px);
        }

        .contact-item i {
            font-size: 1.6em;
        }

        .whatsapp {
            background-color: var(--whatsapp-green);
            color: white;
        }
        .whatsapp:hover {
            background-color: #1DA851; /* أخضر أغمق عند التحويم */
        }

        .email {
            background-color: var(--email-red);
            color: white;
        }
        .email:hover {
            background-color: #C23321; /* أحمر أغمق عند التحويم */
        }

        /* تعديلات للخطوط على الشاشات الصغيرة */
        @media (max-width: 600px) {
            body {
                padding: 20px 10px;
            }
            .contact-card {
                padding: 30px 20px;
            }
            .developer-name {
                font-size: 1.8em;
            }
            .contact-item {
                font-size: 1em;
                padding: 12px 20px;
            }
            .contact-item i {
                font-size: 1.4em;
            }
        }
    </style>
</head>
<body>
    <div class="contact-card">
        <h1 class="developer-name">سعد أبو شندي</h1>
        <p style="color: var(--text-secondary); font-size: 1.1em;">
            يسعدني تواصلكم معي بخصوص استفساراتكم أو مشاريعكم البرمجية.
        </p>

        <div class="contact-info">
            <a href="https://wa.me/+962775379898" target="_blank" class="contact-item whatsapp">
                <i class="fab fa-whatsapp"></i>
                <span>+962775379898</span>
            </a>
            <a href="mailto:saad.abushendi@gmail.com" class="contact-item email">
                <i class="fas fa-envelope"></i>
                <span>saad.abushendi@gmail.com</span>
            </a>
        </div>
    </div>
</body>
</html>
